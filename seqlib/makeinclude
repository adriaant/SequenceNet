.SILENT:

CC = g++ -O4 -funroll-all-loops -fexpensive-optimizations
AR = ar
RM = rm -f
TOUCH = touch

MFLAGS = all

WHERE=`pwd`/
OBJECT_DIR = obj/
LIBRARY_DIR = lib/
INC_DIR = include/
INCLUDE_DIR = -I$(WHERE)$(LEVEL)include/

LO = $(WHERE)$(LEVEL)$(OBJECT_DIR)
LL = $(WHERE)$(LEVEL)$(LIBRARY_DIR)
LI = $(WHERE)$(LEVEL)$(INC_DIR)

AH = $(HOME)
OBJ = obj/
LIB_DIR = lib/
INC_DIR = include/

OPTIONS = -DTARGET_API
